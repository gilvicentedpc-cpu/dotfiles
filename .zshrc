    source $HOME/.zprezto/runcoms/zshrc

# for config_file in $HOME/.zsh/*.zsh; do [ -f "$config_file" ] && source "$config_file"; done

    alias gogit='git add . && git commit -m "update" && git push'
